working_dir: "{{ lookup('env','HOME') }}/.localvm"
base_image_dir: "{{ working_dir }}/base_images"
vm_disk_dir: "{{ working_dir }}/current_images"
vagrant_private_key_path: "{{ lookup('env','HOME') }}/.vagrant.d/insecure_private_key"
vm_resize_partition: "/dev/sda1"
